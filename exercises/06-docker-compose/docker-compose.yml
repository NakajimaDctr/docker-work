services:
  web:                      # サービス名(コンテナ名やホスト名として使われる)
    build: .                # 内部で docker build . 相当の処理を行う
    ports:
      - "8080:5000"         # ホスト:コンテナ のポートマッピング
    depends_on:
      - redis

  redis:
    image: redis:7-alpine   # Docker Hubの既製イメージをそのまま使う 
